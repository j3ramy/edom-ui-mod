package de.j3ramy.edom.commands;

public class AdjustAtm {

    /*
    public AdjustAtm(CommandDispatcher<CommandSource> dispatcher){
        dispatcher.register(Commands.literal("Automat_Auffuellen").then(Commands.argument("value", IntegerArgumentType.integer())
                .executes(commandContext -> fillAtm(commandContext.getSource(), IntegerArgumentType.getInteger(commandContext, "value")))));

        dispatcher.register(Commands.literal("Automat_Leeren").then(Commands.argument("value", IntegerArgumentType.integer())
                .executes(commandContext -> emptyAtm(commandContext.getSource(), IntegerArgumentType.getInteger(commandContext, "value")))));
    }

    private int fillAtm(CommandSource  source, int value) throws CommandSyntaxException {

        ServerPlayerEntity player = source.asPlayer();

        return 1;
    }

    private int emptyAtm(CommandSource  source, int value) throws CommandSyntaxException{

        ServerPlayerEntity player = source.asPlayer();

        return 1;
    }

     */
}
